let name = "Kushal";
console.log(name);
name = "Rabi";
console.log(name);

// Here, as string is immutable(unchangeable) we cannot change its value but we can assign new value to the string as above we first assign kushal as value to the variable(name) and again we assign new string the the same variable(name) we can create new string but we cannot change the existing value of the string. 